#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a;
    printf("enter the year a");
    scanf("%d",&a);
    if(a%4==0)
      printf("the year is the leap year ");
    else
       printf("the year is the not  leap year ");

}
